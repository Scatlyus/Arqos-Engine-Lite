# AE Engine Tests
**Stack**: Jest + fast-check

Unit tests for the core logic layer (AE0-AE3). Must be completely decoupled from external I/O (mock everything).

## Running Tests
```bash
npm test
```
